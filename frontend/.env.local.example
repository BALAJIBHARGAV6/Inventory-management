# Supabase Configuration
# To fix the "Failed to fetch" error and enable database integration:
# 1. Create a Supabase project at https://supabase.com
# 2. Copy your project URL and anon key from the API settings
# 3. Rename this file to .env.local and add your values below

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Example:
# NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Database Tables Required:
# - products (id, name, description, price, category, brand, stock_quantity, image_url)
# - categories (id, name, slug) 
# - user_profiles (id, user_id, full_name, created_at)
# - cart_items (id, user_id, product_id, quantity)
# - orders (id, user_id, total, status, created_at)
# - order_items (id, order_id, product_id, quantity, price)
